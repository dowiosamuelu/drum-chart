# 開發紀錄

逆時序。記「為什麼這樣決定」，不記「改了哪幾行」——那些看 git log。

---

## 2026-08-30（深夜）— 播放不中斷、配過門改用卡片

作者試用後的三個回饋。

### 愛心位置

原本夾在標題和操作按鈕中間，收藏過的卡會有一顆紅心孤零零浮在那裡。
移到**卡片最右邊**（操作按鈕之後），有自己固定的角落。順便把狀態改成**空心 ♡ ／實心 ♥**，
不只靠顏色區分——顏色對色弱使用者不可靠。

### 配過門改用卡片挑

原本藏在編輯彈窗的下拉選單裡。兩個問題：**要進編輯才選得到**，而且**下拉看不到譜**。

改成全域的播放設定 `play.fill`：面板標題列有「配過門」，點了開的是既有的挑卡彈窗——
那裡本來就把每張候選卡的譜畫出來、還能試聽。選好之後，**點任何節奏卡都會用它一起播**。

這個定位是對的：配過門是「我現在想這樣聽」，不是卡片的屬性。所以它不進 localStorage、
重整就回到「不加」。

### 播放不再被重繪打斷

原本 `showView()`、切換節奏／過門、改篩選都會 `stopTransport()`。
但根因不是「應該停」，是**播放器認的是 DOM 元素**（`trans.btn`）——元素一被重繪就失去參照，
所以乾脆停掉。

改成認 key（`card:g8`、`sec:abc`）：
- 播放本身跟 DOM 完全無關，重繪不影響聲音
- 重繪後 `markPlaying()` 用 key 找回元素，把「播放中」的樣子貼回去
- 「同一張再點一次＝停止」也改成比對 key，跨重繪仍然成立

於是切分頁、換篩選、換節奏／過門都不會中斷聲音。**使用者在聽的時候順手改篩選是很正常的事，
聲音停掉會很煩**——這條寫進 CLAUDE.md 了。

---

## 2026-08-30（傍晚）— 內容檢驗與播放語意

上架前先請鼓手（專案作者本人）檢過一輪內容，抓到幾個真正的問題。

### tom-tom 一開始就做錯了

我做的是「落地鼓打八分的 groove」，作者要的是「**用 tom 一路往下打、可能帶切分，當成 groove 而不是過門**」。
兩者都是真的東西，但不是同一個。重做成五張一家族：下行八分（基礎）、下行切分（3-3-2 分組）、
下行＋小鼓 backbeat、Floor tom 平打（原本那張降級成變體）、高低交替。

教訓：**內容的對錯只有實際會打鼓的人判斷得出來**，這不是靠推理能補的。

### 併掉一張重複的卡

「8 beat 十六分踩鈸」和「16 beat 基本」其實是同一張，只差大鼓落點和速度。留後者。

### 補了兩個取樣：crash 與 rimclick

原本沒有這兩個聲音，是取樣的缺口不是譜的缺口，而且對「抒情歌」這個大類影響很大——
**cross-stick 是敬拜抒情主歌最常用的音色**，之前只能用 ghost 頂替，聲音不對。

- `sn` 多一個值 3 = rimclick（它就是打在小鼓上的，不需要新軌）
- 新增 `cr` 軌（crash），排在最上面
- 兩個音檔一樣取自 GrooveScribe，NOTICE 已更新

順手加了兩張用得上新聲音的卡：「抒情 rimclick」、「副歌 ride ＋ 進場 crash」。

### 過門的播放語意改了

原本是「節奏兩小節，然後整整一小節的過門」，但實際打鼓不是這樣——**過門是佔掉樂句最後幾拍**，不是另外接一小節。

改成 `phrase(groove, fill)`：第一小節純節奏，第二小節把過門疊上去。合的規則是
**某一格只要過門有音符，那一格整排就換成過門**（節奏讓位，踩鈸不會疊在 tom 過門上）；過門空著的格子繼續走節奏。

這條規則有個很順的副作用：**過門佔幾拍是它自己決定的**。只在最後一拍有音符的過門，播出來就是「前三拍節奏、第四拍過門」；
佔滿整小節的過門，播出來就是整小節過門。不需要另外設定「這個過門佔幾拍」。

### 收藏

`data.favs` 只存 id，所以合併官方庫不會影響它。側欄「只看收藏」算一種篩選（會切到平鋪結果牆）。

### 還沒解決的

- **過門的「適用段落」標籤語意不清**（是「用在主歌裡」還是「用來結束主歌」？）→ 決定不強制標，過門改用速度感分區。
- **收尾類的過門在單小節格子裡表達不完整**：`簡單收尾（開鈸）` 的重點是「crash 落在下一小節第一拍」，
  但我們只有一個小節，那一下沒地方畫。這是 4/4×16 格架構的結構限制。
- `整小節 tom 下行` 的 tom 分配不均（H×4、M×4、L×6），尾巴那串低鼓可能拖。**待作者聽過決定。**
- 四張卡沒有備註。作者判斷備註沒那麼重要，先擱著。

---

## 2026-08-30（下午）— 變成教會鼓手共享的平台

### 需求前提（決定了整個架構）

- **5 個人**
- **他們不願意辦 GitHub 帳號**
- **兩三個月才審一次**
- 提案只需要「新增卡片」，不需要「修改既有卡」
- 私人卡要保留

第二點直接砍掉所有經過 GitHub 的提案路徑（Issue 預填連結、PR、GitHub API 寫回）。
第三點反而證明手動傳遞是對的——5 個人、一季一次，一輪大概就幾張卡，任何自動化都是在解不存在的問題。

但這兩點合起來冒出一個新風險：**瓶頸不是傳輸，是收件匣**。
有人三月傳一張卡給你、你五月才審，那則 LINE 訊息早就被埋了。所以要蓋的不是管道，是暫存區。

### 資料分成三層

| 層 | 誰能寫 | 怎麼實作 |
|---|---|---|
| 官方庫 | 只有你（實際上是 repo 的 write 權限） | `library.json` 放 repo，GitHub Pages 直接送 |
| 個人庫 | 自己 | localStorage，跟以前一樣 |
| 收到的卡 | 任何人傳給你 | `data.inbox`，分享連結先落在這裡 |

### 三個關鍵決定

**一、官方卡在本地唯讀，想改就先 fork。**
好處是**合併永遠不會衝突**——官方庫單向下發，本地只新增與更新官方卡，不可能覆蓋個人卡。
考慮到一季才同步一次、中間每個人會累積兩三個月的改動，「不可能弄丟東西」遠比「少按一次」重要。
這也讓私人卡的定位更清楚：**個人庫是實驗場，官方庫是定稿。**

**二、官方庫移除某張卡時，本地封存而不刪除。**
不然某人半年前排的歌會突然少一段。封存的卡不出現在瀏覽與挑卡，但 `cardById()` 仍查得到，既有的歌照常運作。

**三、「收到的卡」對所有人一視同仁，不做管理者角色。**
對一般鼓手那是「別人傳給我的」，對你那就是提案匣——同一個東西，零權限系統。
你的權限來自 repo 的 write 存取，不是 app 裡的任何設定。

後來補了一個**管理模式**開關（本機的 checkbox），用來解官方卡唯讀帶來的雞生蛋問題：
維護者需要能編輯官方卡、需要能標記「收進官方庫」、需要匯出 `library.json`。
它**不授予任何權限**，只是把維護用的按鈕露出來——資料本來就是公開的，藏起來也沒有安全意義。

### 實作要點

- seed 從 `index.html` 搬進 `library.json`。內容和程式分家，之後加卡片不用碰那支 900 行的 HTML。
- 啟動流程改成：先用本地資料畫出來 → 背景 fetch `library.json` → 比對版本。
  **不自動合併**（靜默改動別人的東西會嚇到人），顯示橫幅讓使用者按。第一次開啟例外，因為沒東西可以打擾。
- `migrate()` 原本會把「找不到卡的引用」清成 null。官方庫改成非同步載入之後這會**誤殺**範例歌的所有引用，
  所以改成一律保留引用；找不到就顯示「未指定」，載入後自動復原。
- 匯出的 `cardPayload` 不帶 `official` / `archived` / `publish`，那些是本機狀態不是內容。

### 沒做、以及為什麼

- **GitHub Issue 當提案信箱**：需要 GitHub 帳號，直接出局。
- **PAT 從前端寫回 repo**：一季一次的節奏，省下的那個手動步驟不值得引進 token 管理。
- **後端（Supabase / Firebase）**：5 個人、一季一次，完全不需要；而且會推翻「單檔、無 build、無後端」這個核心決策。
  真要上後端，得先確定無後端方案不夠——目前沒有證據。
- **排班／誰打哪一週**：那是另一個產品，跟「傳承打法」無關。明確擋掉。

### 下一步

- **PWA 離線快取**：教會網路常常很爛，而且是現場要用。這件事跟平台化無關但同樣重要。
- 仍然沒做的那件最重要的事：**拿去給真實鼓手用一次**。

---

## 2026-08-30（上午）— 從「攻略表」轉向「節奏庫」

### 定位改變

原本的定位是：以**曲序**為脊椎，把一首歌拆成段落，每段標節奏與過門，匯出 JSON 分享。
問題是它的價值只對編的人成立——是個人筆記，不會隨時間累積成資產。

釐清後的真正痛點是：**新舊鼓手之間沒有辦法傳承「可以打什麼」**。
「這裡可以加點東西」只能用形容詞講，或是打一次給對方看。很多鼓手手上的 8 beat 只有一種打法，
但它可以加 ghost note、可以切分大鼓、可以換成一拍一下、可以搬到 ride 上——這些變化沒有地方存放、沒有辦法指認。

順帶解一個老問題：**主領和鼓手之間沒有共同語彙**。主領說「這裡熱一點」，鼓手不知道是加 ride 還是切 16 beat。
有卡片可以點來聽，溝通就從形容詞變成指認。

所以骨架從「曲序」翻成「節奏庫」，**排進一首歌降級成附加功能**。

### 資料模型 v3 → v4

pattern 從「段落的附屬品」升為第一級公民：

```
patterns: [{ id, name, kind, tags[], parent, note, author, pattern }]
songs:    [{ ..., sections:[{ label, note, grooveRef, fillRef }] }]   // 只存 id
```

`load()` 讀不到 v4 就讀 v3，`fromV3()` 把藏在段落裡的 pattern 抽成卡片、段落改成引用。舊 key 不動。

配套的規則：**段落只能從庫裡挑卡，不能就地編**。想要新打法就去庫裡做一張（或做成某張卡的變體）。
這條規則的用意是讓每次排歌都在餵庫，而不是製造一堆散落在各首歌裡、沒人再看到的 pattern。

### 標籤與家族

- 標籤三組：速度感 / 適用段落 / 打法特徵。篩選是**組內 OR、組間 AND**。
- **沒有 shuffle / 6-8**：16 格的 4/4 打不出三連音，硬塞只會是假的。
- 變體用 `parent` 表示，**只做一層**（在變體上再做變體會掛回同一個 root），避免家族樹失控。
- A/B「輪流播」兩張各兩小節交替，**共用同一個速度**——不然差別會被速度差蓋掉。

### 介面：四輪迭代

一開始是三欄（篩選｜清單｜編輯器），卡片只有名稱和標籤。第一次實際看畫面就發現不對：
**要點進去才知道譜長怎樣**，那「一眼看到有哪些打法」的目的等於沒達成。

於是譜改成直接顯示，之後一路收斂：

1. **譜攤在卡上** — 卡片牆、挑卡彈窗、曲序段落都畫譜。
   `miniGridHtml()` **只畫有打的軌**，卡片才不會被三行空白軌撐高。編輯移進彈窗，卡片牆不會因為編輯而重排。
2. **拿掉格子框線** — 48 個方塊裡 33 個是空的，空盒子跟音符搶一樣多的視覺重量。
   改成只畫音符，每拍一條淡線貫穿當對齊骨架。
   （討論過改用五線譜。**否決**：目標讀者是自學的教會鼓手，讀譜比例不高；而且五線譜和點格子編輯是兩套表示法，
   要維護兩份渲染邏輯。真要做就當**選配開關**，不是取代品。）
3. **層級修正** — 上一步之後按鈕接手變成最大的噪音源（25 張卡 × 4 顆有框按鈕）。
   - 操作按鈕平常隱藏、hover 才出現
   - **點整張卡就是播放**，「試聽」按鈕整顆拿掉（高頻動作不該長得跟低頻動作一樣）
   - 標籤從 pill 收成一行 `中板 · 主歌 · 8 beat`（順帶解掉「側欄 pill 可點、卡片 pill 不可點但長一樣」的混淆）
   - 標題與備註單行截斷 → 卡片等高。不整齊本身就是雜訊
   - 小譜的墨水濃淡對應資訊重量：踩鈸／ride 是織體所以調淡，小鼓與大鼓是骨架所以維持深
   - 修掉一個 bug：`overflow-x` 不是 `visible` 時 `overflow-y` 會隱含變成 `auto`，於是每張譜都長出一條垂直捲軸
4. **主題換成 Linear／Notion 那套中性淺色** — 原本的暖調紙感把強調色用得到處都是，本身就是層級混亂的來源。
   改成近白底、髮絲線、單一靛紫強調色只用在「選中／播放中／主要動作」。

### 首頁：分類入口，不是全庫平鋪

考慮過的方案：全展開（現狀）／家族收合／收合但列出變體名字／摘要模式切換／**分類入口**。

選了分類入口：把標籤分類直接變成瀏覽結構，每類先給三張代表卡＋「看全部」。
第一屏回答的是「節奏分成哪幾類、每類長怎樣」，而不是「這裡有 21 張卡」——**新鼓手是主要受眾**，
他需要先知道有哪些類別存在。老鼓手用搜尋和篩選直接跳過這層。

原本評估「這是庫長到 60 張以上時的答案，現在做有點早」，顧慮是庫小時分區會顯得空；
用「空的分區不顯示」擋掉。

行為：沒搜尋也沒篩選 → 分區瀏覽；一有搜尋或標籤篩選 → 平鋪結果牆。「看全部」就是把該標籤套成篩選。

### 家族的視覺表達

變體原本用 `margin-left` 縮排。問題是它跟 CSS columns 的瀑布流衝突——縮排要有意義得有個「沒縮排的東西」
在旁邊當參照，但家族被排到某一欄最上面時，那張變體上面什麼都沒有，位移就沒有來源。

改成：整組變體包進一個容器，**共用一條連續的左側軌**（線本身就是參照）；變體底色下沉到頁面色，
讀起來是凹進去的次要層。

考慮過但沒做：把家族包成一張大卡、變體變成卡片頂端的分頁（切著看、譜的位置不動）、差異疊加（diff）。
**分頁那個值得留著**——譜的位置不動、只有變的那幾格在變，是展示「差在哪」最有效的形式。

### 還沒驗證的

- 這輪所有改動都只跑過 headless 的結構測試（59 條），**沒有在真的瀏覽器裡渲染過**（開發機沒有 playwright）。
  配色、hover 手感、分區格線的斷行都要人眼確認。
- 內建 26 張卡的譜是按常見寫法打的，**還沒有真的鼓手驗收過**。
- 最重要的那件事仍然沒做：**拿去給真實鼓手用一次**，確認「聽得到差別」真的有幫助。
  這比再寫任何新功能都重要。
