# CLAUDE.md — 鼓手節奏庫（Drum Chart）

## 這是什麼
小圈子鼓手共用的**節奏庫**：把「可以打什麼節奏、可以用什麼過門」變成一張張可以**點下去就聽到**的卡片，
讓老鼓手把手上的打法傳承給新鼓手——不用再用形容詞比手畫腳，直接指一張給他聽。
同一個 8 beat 常常只有一種打法在流傳，但其實可以換裝飾、換打點、換音色位置；卡片的**變體家族**就是在給這件事一個位置。
「把卡片排進一首歌的曲序」是**附加功能**，不是主軸。

真實情境：教會敬拜，主領一週開四首歌；面向華語鼓手，補中文鼓譜資源的空缺。

## 架構（刻意極簡）
- **`index.html` ＋ `library.json`**：純前端、無框架、無後端、無 build。GitHub Pages 直接送。
  程式在 `index.html`，**官方庫的內容在 `library.json`**（加卡片不用碰程式）。個人資料存 `localStorage`（key `drumchart_v4`）。
- **資料三層**：
  - **官方庫**：`library.json`，只有 repo 的 write 權限能改。啟動時背景 fetch，比對 `data.libVersion`，
    **有新版不自動合併**，顯示橫幅讓使用者按（第一次開啟例外，沒東西可打擾）。
  - **個人庫**：`data.patterns` 裡 `official:false` 的卡，各自的實驗場。
  - **收到的卡**：`data.inbox`，分享連結一律先落在這裡，聽過再決定收不收。
- **管理模式**（`data.admin`，本機 checkbox）：解開官方卡唯讀、顯示「收進官方庫」與「匯出 library.json」。
  **它不授予任何權限**——真正的權限是 repo 的 write 存取。資料本來就公開，藏起來沒有安全意義。
- **測試**：`node test.js`（83 條，headless、假 DOM、假 fetch）。改動前後都跑一次。
- **資料模型（v4）**：pattern 是第一級公民。
  ```
  patterns: [{ id, name, kind:"groove"|"fill", tags[], parent, note, author, pattern }]
  songs:    [{ id, name, author, ytUrl, videoId,
               sections:[{ id, label, note, grooveRef, fillRef }] }]   // 段落只存卡片 id
  ```
- **鼓譜格式**：`pattern = { tempo, cr[], ri[], oh[], hh[], tm[], sn[], kk[] }`，每軌 16 格（4/4、十六分）。
  - `tm`：0空 / 1 H / 2 M / 3 L（高中低 tom）
  - `sn`：0空 / 1 重音 / 2 ghost / 3 rimclick（cross-stick）
  - 其餘軌（`cr` crash、`ri` ride、`oh` open hi-hat、`hh` closed hi-hat、`kk` kick）：0 / 1
  - 軌順序常數見 `LANE_KEYS` 與 `LANES`；內建卡片用 `P("1010...")` 字串寫譜，比陣列好讀。
- **標籤**：`TAG_GROUPS` 三組——速度感 / 適用段落 / 打法特徵。篩選是**組內 OR、組間 AND**。
- **畫面**：兩塊——左邊篩選側欄（sticky），右邊有**兩種模式**（`renderGallery()` 依 `filtering()` 決定）：
  - **分區瀏覽**（首頁，沒搜尋也沒篩選時）：`renderBrowse()` 依 `axis` 把卡片分區，每區最多 `MAX_PER_SECTION` 張代表卡＋「看全部」。
    分區軸可切（速度感／適用段落／打法特徵）；節奏預設「打法特徵」、過門預設「適用段落」（過門沒有打法特徵標籤）。
    **空的分區不顯示**，庫小的時候才不會一片空白。
  - **平鋪卡片牆**（一有搜尋或標籤篩選）：`renderWall()`，CSS `columns`，一個家族一個 `.fam` 區塊、`break-inside:avoid`。
  - `cardHtml(c, opt)` 是共用的卡片元件（名稱＋BPM＋標籤＋譜＋備註＋按鈕），卡片牆、挑卡彈窗都用它；`opt.buttons` 可換掉按鈕組。
  - `miniGridHtml(p)` 是唯讀小譜：**只畫有打的軌**，而且**只畫音符不畫格子**——空拍留白，每拍一條 `--faint` 直線貫穿整張譜當對齊參考。
    空盒子跟音符搶視覺重量，所以拿掉；拍線是格子譜唯一真正需要的骨架。`gridHtml(p)` 是彈窗裡的可編輯大格子，六軌全開、有框線（那是點擊目標，需要看得見）。
  - 編輯在 `#cardDialog` 彈窗裡做，卡片牆不會因為編輯而重排。
- **播放**：Web Audio 載入 `samples/*.mp3`（真鼓取樣）觸發；`playBuf()` + `GAINS` 表做各鼓件音量微調（open hi-hat 壓到 0.5）。
  排程器是 `playSeq(items, btn, tempo)`（25ms lookahead），`items = [{p, bars}]` 循環播放。
  A/B 輪流播與樂句播放都是靠塞多段進 `playSeq`；**整串共用同一個 tempo**，差別才聽得出來。
- **播放器認 key 不認 DOM 元素**（`trans.key`，例如 `card:g8` / `sec:abc`）。元素會因為重繪而消失，
  認 id 才能**跨重繪繼續播**——切分頁、換篩選、換節奏／過門都不會中斷聲音，重繪後 `markPlaying()` 把樣子貼回去。
- **「配過門」是全域播放設定**（`play.fill`），不是卡片的屬性：用挑卡彈窗選一張（看得到譜、聽得到），
  之後點任何節奏卡都會用它一起聽。選過門用卡片不用下拉——下拉看不到譜。
- **樂句播放**：`phrase(groove, fill)` = 第一小節純節奏，第二小節是 `mergedBar()` 合出來的「節奏＋過門」。
  合的規則是**某一格只要過門有東西，那一格整排就換成過門**（節奏讓位，踩鈸不會疊在 tom 過門上）；
  過門空著的格子繼續走節奏。所以只佔最後一拍的過門自然就是「前三拍節奏、第四拍過門」。
- **收藏**：`data.favs` 只存 id，跟卡片內容無關，所以合併官方庫不會影響它。側欄「只看收藏」算一種篩選。
  ♥ 固定在卡片右上角（操作按鈕之後），空心／實心區分狀態，不只靠顏色。
- **遷移**：`load()` 先讀 v4（`migrate()` 補欄位、清掉失效的 parent），沒有就讀 v3（`fromV3()` 把藏在段落裡的 pattern 抽成卡片），再沒有就 `defaultData()`（空庫）。
  **`migrate()` 不會清掉找不到卡的引用**——官方庫是非同步載入的，清掉會誤殺；找不到就顯示「未指定」，載入後自動復原。
- **分享**：整庫 / 單首歌 JSON 匯出匯入（歌會**夾帶用到的卡片與其父卡**）＋ fork；**單張卡走網址** `#c=<base64>`（一張卡約 370 字元，塞得進 URL）。

## 重要設計決策（不要回頭推翻）
- 骨架是**節奏庫**，不是曲序、不是時間軸。曲序只是把庫裡的卡排起來。
- **官方卡在本地唯讀**，想改就先「做我自己的版本」。這讓合併永遠不會衝突——單向下發，不可能覆蓋個人卡。
  一季才同步一次，中間每個人會累積兩三個月的改動，「不可能弄丟東西」比「少按一次」重要得多。
- **官方庫移除某張卡時封存而不刪除**，不然別人半年前排的歌會突然少一段。
- **不做管理者角色**。「收到的卡」對所有人一視同仁；權限來自 repo，不是 app。
- **不做後端**、不用 GitHub Issue／API 收提案（鼓手不願意辦 GitHub 帳號）、不做排班（那是另一個產品）。
- 過門**不強制標「適用段落」**——那個標籤對過門語意不清（是「用在主歌裡」還是「用來結束主歌」？），
  所以過門的預設分區軸是「速度感」。
- **首頁是分類入口，不是全庫平鋪**：把標籤分類直接變成瀏覽結構，第一屏回答的是「節奏分成哪幾類、每類長怎樣」，
  而不是「這裡有 21 張卡」。新鼓手是主要受眾，他需要先知道有哪些類別存在。老鼓手用搜尋和篩選直接跳過這層。
- **譜一律直接顯示**，不藏在「點進去才看得到」後面——卡片牆、挑卡彈窗、曲序段落都畫譜。
  這是這個工具的重點：一眼掃過去就知道每張卡長怎樣，不用逐張點開。
- **不做五線譜**（至少現在不做）。目標讀者是自學的教會鼓手，讀譜比例不高，格子譜不用學就看得懂；
  而且五線譜和「點格子編輯」是兩套表示法，要維護兩份渲染邏輯。真要做就當成**選配開關**，不是取代品。
- 段落**只能從庫裡挑卡**，不能就地編。想要新打法就去庫裡做一張（或做成某張卡的變體）——這條規則讓每次排歌都在餵庫。
- **變體只做一層**（`parent` 指向 root，不做巢狀），避免家族樹失控。
- 編輯**就地點格子**，不依賴外部工具（早期版本曾外連 GrooveScribe，已移除；僅借用其取樣音檔放進 `samples/`）。
- 拍號**只做 4/4 / 16 格**。因此標籤裡**沒有 shuffle / 6-8**——16 格打不出三連音，硬塞只會是假的。
- 合成鼓「太假」→ 改用真鼓取樣；要更好就換更高品質 sample，不要走回合成。
- **不做主領專用視圖**。主領靠鼓手點卡給他聽就夠了；給主領看格子兩邊都不討好。
- YouTube 是選配參考，可收合、lazy mount；**不做**與鼓聲的時間軸同步。

## 怎麼跑
`python3 -m http.server 8777` 後開 http://localhost:8777/index.html（不能用 file://；`library.json` 和 `samples/` 都要 fetch）。
測試：`node test.js`。

## 下一步候選（按價值）
1. **先驗證**：拿內建的 26 張卡去給真實鼓手點點看，確認「聽得到差別」真的有幫助——這比寫新功能重要。
2. **補內容**：庫的價值 = 裡面有幾張好卡，不是編輯器多好用。缺哪些打法只有實際使用者知道。
3. 真正的**雲端資料庫**（取代 JSON 快照、讓庫可以共同長大）——迴圈確認有價值後才做。
4. 每軌音量／拍號擴充；更真的音色。

## 風格
- 介面走 **Linear／Notion 那種中性淺色**：近白底、髮絲線邊框、6–8px 圓角、極淡陰影、
  單一強調色（靛紫 `--accent:#5b5bd6`）只用在「選中／播放中／主要動作」，其餘一律灰階。
  色票集中在 `:root`，不要在個別規則裡硬寫顏色。
- **視覺重量要對應資訊重量**：譜是內容，最重；操作是次要，**平常隱藏、hover 才出現**；
  標籤是後設資料，退成一行小字（`中板 · 主歌 · 8 beat`）而不是一堆 pill。
  小譜裡踩鈸／ride 是織體所以調淡，小鼓與大鼓是骨架所以維持深，ghost 更淡。
- **高頻動作不要按鈕**：點整張卡就是播放／停止（播放中整張卡變色）。
- **不要為了重繪而中斷聲音**。使用者在聽的時候切分頁、改篩選都很正常，聲音停掉會很煩。
- 卡片高度要齊：標題單行截斷、備註單行截斷——不整齊本身就是一種雜訊。
- **不用裝飾性 emoji**（只留 ▲▼▸✕ 功能箭頭）。
- 回覆與註解用繁體中文。
